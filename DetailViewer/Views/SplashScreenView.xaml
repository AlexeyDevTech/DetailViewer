<Window x:Class="DetailViewer.Views.SplashScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Height="200" Width="400">
    <Border CornerRadius="10" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Загрузка приложения..." FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding StatusText}" FontSize="14" Foreground="Gray"/>
            <ProgressBar IsIndeterminate="True" Width="300" Height="15" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</Window>
