<UserControl
    x:Class="DetailViewer.Modules.Dialogs.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="300"
    d:DesignWidth="300"
    Height="400" Width="600"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Text="Database Path:" />
            <TextBox IsReadOnly="True" Text="{Binding DatabasePath}" />
            <Button Command="{Binding ImportCommand}" Content="Import from Excel" />
            <Button Command="{Binding ExportCommand}" Content="Export to Excel" />
        </StackPanel>
    </Grid>
</UserControl>