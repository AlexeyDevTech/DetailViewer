
<UserControl x:Class="DetailViewer.Modules.Dialogs.Views.ConflictResolutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Обнаружен конфликт!" FontSize="18" FontWeight="Bold" Margin="10" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Ваша версия" Margin="5">
                <ContentControl Content="{Binding LocalEntity}" />
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Серверная версия" Margin="5">
                <ContentControl Content="{Binding RemoteEntity}" />
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Оставить мою версию" Command="{Binding KeepLocalCommand}" Margin="5" />
            <Button Content="Принять серверную версию" Command="{Binding KeepRemoteCommand}" Margin="5" />
            <Button Content="Отложить" Command="{Binding PostponeCommand}" Margin="5" />
        </StackPanel>
    </Grid>
</UserControl>
